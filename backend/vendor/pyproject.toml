[project]
name = "emergentintegrations"
version = "0.1.0"
description = "Local stub of Emergent private package for Rip N' Flip"
requires-python = ">=3.10"
dependencies = ["stripe", "pydantic"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["emergentintegrations"]
